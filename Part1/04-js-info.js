"use strict"; // used to treat all js code as newer version of js

console.log("Tarun Pal"); // Tarun Pal (But not recommended)

// console.log("
//     Tarun Pal
// ");  // error

console.log(3 + 3); // 6

console.log(`Tarun `); console.log(`Pal`); // not to do like this

// alert("Hello!"); // error because alert is not the part of js, it is the part of window object that's why we get this error. We have a different method to use alert in node.js

/* 
// Simple pop-up

npm install node-notifier

const notifier = require('node-notifier');

notifier.notify({
    title: "Alert!",
    message: "This is a custom message from node.js"
});
*/
